# Development Seed Data

The JSON files in this folder contain **small example datasets** used during development.

They are NOT the full monster or template libraries.

They exist to:

- define the expected data structure
- allow the generator to run during early development
- help Copilot infer the correct data types and filtering logic

Later versions of the application may import larger datasets generated
from external sources such as Excel.
