# Development Seed Data

The JSON files in this folder contain **minimal example datasets** used during development.

They are not intended to represent the full monster library or all encounter templates.

They exist to:

- define the expected data structure
- provide sample data for testing the generator
- help Copilot infer correct types and filtering logic

These files may later be replaced with larger generated datasets.
